# SIMPS
SIMPS (Smart Investment Management &amp; Portfolio System) is a full‑stack web application built using Django REST, PostgreSQL, React, and Tailwind CSS to manage user finances, calculate savings, and simulate investment portfolio creation.

# branching
```
# your-branch-name: (shaswatbranch / prayushbranch / rhikibranch)
git checkout your-branch-name

# After making changes
git add .
git commit -m "Your descriptive message"
git push origin your-branch-name

# Pull latest changes from main
git checkout main
git pull origin main

# Merge main into your branch
git checkout your-branch-name
git merge main
```
